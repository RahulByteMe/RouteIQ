import { CITY_PRESETS } from "./cityPresets";

// Default initial stops from the primary city preset (New York)
const initialStops = CITY_PRESETS[0].stops;

export default initialStops;
export { CITY_PRESETS };